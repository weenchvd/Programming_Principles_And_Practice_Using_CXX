/* Exercise 18.4 */

using namespace std;

char* str_dup(const char* s, int n = numeric_limits<int>::max());
char* find_x(const char* s, const char* x, int n = numeric_limits<int>::max());
int str_cmp(const char* s1, const char* s2, int n = numeric_limits<int>::max());
void ClearInput(istream& is);
